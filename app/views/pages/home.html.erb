<!DOCTYPE html>
<html>
<head>
  <title>Top 3 Restaurantes</title>
</head>
<body>
  <h1>Top 3 Restaurantes con más items</h1>
  <div class="container">
  <%= Restaurant.select('restaurants.*, COUNT(items.id) AS item_count')
                                 .joins(:items)
                                 .group('restaurants.id')
                                 .order('item_count DESC')
                                 .limit(3)
                                 .each do |restaurant|%>
        <h5><%= restaurant.name %></h5>
        <h6><%= restaurant.items.count %></h6>
    </div>
    <%end%>
</body>
</html>